const initialState = {
  rowCount: 3,
  columnCount: 3,
  stepCount: 10,
};

export const settings = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
